{
  "name": "rome",
  "version": "0.2.0",
  "description": "基于公网 MQTT 中转的远程命令行桥接工具，支持端到端密封消息。",
  "bin": {
    "rome": "./bin/rome.js"
  },
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "start": "node bin/rome.js",
    "dev": "tsc --watch",
    "test": "npm run build && node test/config.test.js && node test/auth.test.js"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^11.1.0",
    "mqtt": "^5.15.1",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
